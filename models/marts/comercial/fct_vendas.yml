version: 2

models:
  - name: fct_vendas
    desciption: Tabela com os fatos de vendas da northwind.
    columns:
      - name: id_pedido
        description: A primary key da dimensão de produtos.
        tests:
          - unique
          - not_null